# Word temp files
~$*
~WRL*
*.tmp

# Original template (not needed in repo)
modeloPT.docx
"Plano de Trabalho - Projeto Lity v2.3.docx"

# Claude config
.claude/

# Deploy (server-only files)
deploy.log
deploy.sh
.env
webhook/hooks.json
